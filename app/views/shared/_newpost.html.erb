<div class="container">
  <div class="row">
    <h2 class="col-10">New book</h2>

    <div class="col-10">
    <%= form_with model: book do |f| %>
      <label>Title</lavel>
      <%= f.text_field :title, class: "form-control mb-3" %>

      <lavel>Opinion</label>
      <%= f.text_area :body, class: "form-control mb-3" %>

      <%= f.submit "Create Book", class: "btn btn-success" %>
    <% end %>
    </div>
  </div>
</div>
